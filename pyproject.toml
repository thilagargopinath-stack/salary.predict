[project]
name = "salary-predict"
version = "0.1.0"
description = "Salary Prediction Web App"
requires-python = ">=3.9"
dependencies = [
    "Flask>=2.0.0",
    "Flask-Cors>=3.0.0",
]

[tool.vercel]
entrypoint = "api.predict:app"
